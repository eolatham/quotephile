version: "3"
services:
  database:
    image: postgres
    env_file:
      - db.env
    volumes:
      - ./db.sql:/docker-entrypoint-initdb.d/db.sql # Build the database schema on startup
    ports:
      - 5432:5432
